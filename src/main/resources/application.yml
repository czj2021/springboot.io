spring:
  datasource:
    url: jdbc:mysql://localhost:3306/book?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

#  jpa:
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true

mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.springboot.book.entity

server:
  port: 8181

static:
  imagePath:
    file:D://img/

logging:
  level:
    com.southwind.springboottest.mapper: debug



